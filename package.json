{
  "name": "kassette-playground",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "playwright test --config=playwright.config.js",
    "test:proxy": "cross-env USE_PROXY=true DEBUG=pw:webserver playwright test --workers=1 --config=playwright.config.js",
    "proxy": "kassette -c kassette.config.js",
    "serve": "http-server ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@amadeus-it-group/kassette": "^1.6.0",
    "@playwright/test": "^1.32.3",
    "cross-env": "^7.0.3",
    "http-server": "^14.1.1"
  }
}
